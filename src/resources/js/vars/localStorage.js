const prefix = `${import.meta.env.VITE_APP_NAME}-`;

export const TOKEN_KEY = `${prefix}token`;

export const SIDEBAR_KEY = `${prefix}sidebar`;

export const VALIDATION_KEY = `${prefix}validation`;

export const MULTI_KEY = `${prefix}multi`;

export const DARK_MODE = `${prefix}darkmode`;

export const OPEN_INTRUCTION = `${prefix}intruction-opened`;
